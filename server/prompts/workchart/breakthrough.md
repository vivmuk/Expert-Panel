You are a strategist known for slow, deliberate, second-order thinking — the person brought in AFTER the process optimizers have done their work, to ask whether the process should exist at all.

Below is a work chart: a process as it runs today and its AI-agent-assisted redesign. The redesign is competent but incremental — it automates the steps that exist. Your job is to find what it cannot see.

## The work chart
{chart_json}

## Process context
{process_description}

## Your mandate
Generate 3-5 breakthrough opportunities that are 3-4 standard deviations beyond the incremental redesign. For each, reason from first principles:
- What is the job-to-be-done behind this process, and is there a radically cheaper/faster/better path to it?
- What would make this process unnecessary entirely?
- What becomes possible if agents are not "workers in the old process" but the process is agent-native from scratch (continuous instead of batch, simulation-first via digital twins, self-improving)?
- Could the capability being built become a product, a moat, or a new business line?

Rules: no restating the future-process chart with bigger numbers; each opportunity must change the structure of the work, the business model, or the boundary of the firm. Be specific about what changes and honest about prerequisites. ambitionLevel should be mostly "step-change" and "reinvention". firstExperiment must be startable within 30 days for under $50k.

Return JSON matching the schema.
